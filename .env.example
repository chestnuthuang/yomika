# OpenAI API key (required)
OPENAI_API_KEY=sk-proj-your-key-here

# Local server port
PORT=8787

# false = Reader only on this computer (recommended default)
# true  = allow read-only Reader access from devices on the same LAN / Wi-Fi
# Translation/API-spending endpoints remain localhost-only.
ENABLE_LAN_READER=false
